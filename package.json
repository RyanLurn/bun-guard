{
  "name": "bun-guard",
  "type": "module",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "typescript": "^5.9.3"
    },
    "catalogs": {
      "types": {
        "@types/bun": "^1.3.4"
      }
    }
  },
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "dev": "bun run --bun --filter '*' dev",
    "type-check": "bun run --bun --filter '*' type-check",
    "watcher:dev": "bun run --bun --filter @bun-guard/watcher dev"
  },
  "devDependencies": {
    "lefthook": "^2.0.9",
    "prettier": "3.7.4",
    "prettier-plugin-curly": "0.4.1"
  }
}
